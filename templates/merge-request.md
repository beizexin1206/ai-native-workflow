<!-- 六段都要填。What/How/Scope/Tests 由 agent 从仓库产物汇总；
     Why 来自 docs/intent/ —— 找不到出处就报缺口，不要从 diff 反推。 -->

## What

<一句话：这个 MR 让系统发生了什么变化。>

Meegle: [PROJ-1234](<工作项链接>)　·　SPEC: `SPEC.md`

## Why

<为什么这么做、否决了哪些方案。>
<出处：`docs/intent/<主题>.md` / `tasks/plan.md#方案选型`>

<!-- 这一段是整个 MR 里唯一无法从代码读出来的东西。
     写「因为需求要求」等于没写 —— 要写的是「有 A/B 两条路，选了 B，因为……」。 -->

## How

<实现路径与关键取舍。对应 tasks/plan.md 的哪几个阶段，commit 范围是哪些。>

## Scope

<改了哪些模块 / 接口 / 数据。>

- [ ] 本次是否改变了 `AGENTS.md` 描述的行为？改了 → 已同步更新
- [ ] 是否改变了对外接口 / 数据格式？改了 → 已通知上下游、已写迁移或兼容
- [ ] 是否有不可逆操作（迁移、删除、协议变更）？有 → 回滚路径见下

## Tests

<跑了什么、结果如何。「测试通过」不是结论。>

- 新增用例：<覆盖了验收标准的哪几条>
- 全量回归：<跑了 N 条，挂 M 条，原因与处置>
- 手工验证：<做了什么，没有就写「无」>

## 注意点

<风险、坑、留下的技术债及其上限、被否决但值得记录的方案、需要 reviewer 特别看的地方。>
<没有就写「无」—— 但先想三十秒再写「无」。>

---

<!-- 合入前：所有 discussion 已 resolve，CI 绿。
     未 resolve 的讨论不得合入 —— 每条只有两种结局：修掉，或带理由不修，两种都要回复。 -->
