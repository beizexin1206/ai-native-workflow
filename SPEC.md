# SPEC: 强化质量报告的人工 Review 决策信息
> Requirement ID: quality-report-review-contract
> Source: 用户对测试团队原始模板与当前公共模板的 Review 结论

## 原始需求

测试团队希望质量报告保持原有人工 Review 习惯；AI 变革小组不复刻原报告，而是在公共模板中保留影响人工判断的关键信息，将重复用例、缺陷档案、原始日志和特定 Agent 编排下沉到各自事实源。

## 目标

让 Reviewer 能从公共质量报告直接判断测试范围、证据可信度、需求偏差、残留风险和上线条件，同时保持报告简洁、通用、可追溯。

## 可验证行为

- 质量报告按 P0/P1/P2 展示计划数与 PASS、FAIL、BLOCKED、NOT RUN 分布，并展示登记缺陷数。
- 报告显式列出验证方式降级或能力缺口；没有例外时明确写“无”。
- 需求偏差包含人工裁决来源、后续动作、负责人和期限。
- 缺陷仅保留支持本轮上线判断的摘要与稳定链接，不复制完整缺陷档案。
- 残留风险包含影响、证据强度、闭环状态、负责人和期限。
- 多仓、接口、schema 或独立部署变更可以使用可选的联合发布评估区；普通改动不被迫填写大段空内容。
- `/t` 的生成规则与 HTML 模板口径一致。

## 边界与非目标

- 不恢复逐条完整测试步骤、完整请求响应、截图或日志正文。
- 不把 Reporter、Executor、打回次数等特定 Agent 编排设为公共必填项。
- 不改变 `test-cases.md` 作为稳定测试设计基线的职责。
- 不增加新的外部系统、依赖或自动发布行为。

## 影响面

| 维度 | 影响 |
|---|---|
| 模块 / 接口 | `templates/quality-report.html`、`skills/t/SKILL.md`、模板契约检查 |
| 数据 / 存储 | 无 |
| 上下游 | `/init` 安装模板的消费仓库需要重新执行 `/init` 才能取得新版模板 |

## 待澄清

- 无。
